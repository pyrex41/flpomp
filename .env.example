# X (Twitter) API — Required for posting
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=

# Application — Optional (defaults shown)
PORT=8080
DATA_DIR=./data

# Pomelli — Website URL for Business DNA generation
WEBSITE_URL=https://yoursite.com

# Security — Set to enable basic auth on dashboard
ADMIN_PASSWORD=
